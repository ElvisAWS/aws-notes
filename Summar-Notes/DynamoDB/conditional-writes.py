

# Concurrent Writes
    # One of the request will over write the other
# conditional Writes
    # We can apply optimistic locking here
# Atomic writes
    # both writes will succeed
# Batch writes
    # Updating many items at a time